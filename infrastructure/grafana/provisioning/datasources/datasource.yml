apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # 'prometheus' matches the container name in docker-compose.yml
    url: http://prometheus:9090
    isDefault: true
    editable: false
