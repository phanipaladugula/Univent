apiVersion: 1

providers:
  - name: 'Univent Default Dashboards'
    orgId: 1
    folder: 'Univent Services'
    type: file
    disableDeletion: false
    editable: true
    options:
      # This points to the location of dashboard JSON files mapped inside the Grafana container
      path: /etc/grafana/dashboards
